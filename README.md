# myRepo

This repository contains a simple Java number guessing game.

## Building and Running

Compile the program using `javac` and run it with `java`:

```bash
javac src/GuessNumber.java
java -cp src GuessNumber
```

The program generates a random number between 1 and 100 and keeps track of how many attempts you take to guess correctly.
